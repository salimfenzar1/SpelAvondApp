name: Build and Deploy ASP.NET Core to Azure Web App

on:
  push:
    branches:
      - master  # Of de branch die je wilt gebruiken voor deployment

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.x' 

    - name: Install dependencies
      run: dotnet restore

    - name: Build the project
      run: dotnet build --configuration Release --no-restore

    - name: Run tests
      run: dotnet test --no-restore --verbosity normal

    - name: Publish project
      run: dotnet publish --configuration Release --no-restore --output ./publish

    - name: Login to Azure
      uses: azure/login@v1
      with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_78E7E83FA31945D89D35DD2390AC8158 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_16C5E2C4DE5F446C95AC2CBD050CB65C }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_94DFE0AAA7A54EAFACB1F4F45DF49E28 }}   

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'bordspelavond'  
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_DB881BEC0F3E436B89313302765D2C32 }}
        package: .
