@model IEnumerable<SpelAvondApp.Domain.Models.BordspellenAvond>

@{
    ViewData["Title"] = "Bordspellenavonden Overzicht";
}

<h1>@ViewData["Title"]</h1>
<a asp-controller="BordspellenAvond" asp-action="Create" class="btn btn-success mb-3">Bordspel Aanmaken</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Datum en Tijd</th>
            <th>Adres</th>
            <th>Organisator</th>
            <th>Max Spelers</th>
            <th>18+</th>
            <th>Bordspellen</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var avond in Model)
        {
            <tr>
                <td>@avond.Datum.ToString("dd-MM-yyyy HH:mm")</td>
                <td>@avond.Adres</td>
                <td>@avond.Organisator?.UserName ?? "Onbekend"</td>
                <td>@avond.MaxAantalSpelers</td>
                <td>@(avond.Is18Plus ? "Ja" : "Nee")</td>
                <td>
                    @if (avond.Bordspellen != null && avond.Bordspellen.Any())
                    {
                        <ul>
                            @foreach (var bordspel in avond.Bordspellen)
                            {
                                <li>@bordspel.Naam</li>
                            }
                        </ul>
                    }
                    else
                    {
                        <span>Geen bordspellen toegevoegd</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
